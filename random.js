// to generate a random number.
// limit number without incluiding it, it will never be 6. It is like the range
var limitNumber = 6
var x = Math.floor(Math.random() * limitNumber);

console.log(x)
